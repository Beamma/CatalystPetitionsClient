What is best way to handle nav bar, with users and ids? When I go to the route via url, it loses the id from the store.
Does it need to be reactive to mobile


Need to display default images if 404
Fix users images displaying on supporters